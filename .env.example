# Adhara workspace this site is wired to.
ADHARA_BASE_URL=https://api.adharaweb.com
ADHARA_WORKSPACE_ID=your-workspace-slug-or-id

# Server-to-server API key (Dashboard → Settings → API Keys). Used for the
# contact form, survey, and join/checkout routes — not for portal customer
# login/signup, which authenticates as the customer instead.
ADHARA_API_KEY=adhara_pk_xxxxxxxxxxxxxxxxxxxxxxxx

# Adhara Contact Us form ID, used as a lead-capture backup in app/api/join.
ADHARA_CONTACT_FORM_ID=

# Stripe price ID for the Founding Membership, used to start checkout in app/api/join.
ADHARA_MEMBERSHIP_PRICE_ID=

# Debugging/testing only: view /dashboard without a real Adhara login (shows
# a fake "Madison (Preview)" user). Always on in `next dev` regardless of
# this flag. Set to "true" here to also enable it on a `next build && next
# start` run (e.g. a preview deployment) — leave unset on the real production
# deploy. A real login always takes priority over this when one exists.
AUTH_BYPASS=

# Roadmap tool database (app/lib/db). Local dev: `npm run db:up` starts a
# Docker Postgres matching this URL exactly. Production: a Neon connection
# string (use the pooled one here; see DATABASE_URL_UNPOOLED for migrations).
DATABASE_URL=postgres://bodyunmuted:bodyunmuted@localhost:5432/bodyunmuted
DATABASE_URL_UNPOOLED=

# Comma-separated Adhara account emails treated as the coach/admin role.
# Adhara has no coach/member concept itself — this is the only place that
# distinction is made.
ADHARA_COACH_EMAILS=
